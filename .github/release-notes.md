账户「历史记录」范围切换改为下拉框，样式与整体风格统一。

- 「近 7 天 / 近 30 天」按钮组改为下拉框，复用模型筛选的圆角、箭头与弹出列表样式。
- 深浅双主题下配色一致，刷新后保持当前所选范围。
- 历史明细表格与合计口径不变。

下载 `CodexUsage-Windows.zip`，解压后运行 `CodexUsage.exe`。更新前先退出旧版。

构建与自动回归测试通过；安装包不包含账号信息、本机日志或诊断文件。
